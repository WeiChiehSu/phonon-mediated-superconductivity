引入Born-Oppenheimer approximation後,電子的Hamilton被簡化為:

After introducing the Born–Oppenheimer approximation, the electronic Hamiltonian is simplified as:

$$
\widehat{H}_{electron}= -\sum_{i=1}^{n} \frac{\nabla^{2} _{r_{i}  }  } {2} +\sum_{i=1 }^{n} \sum_{j>i }^{n} \frac{1 }{r_{ij}}+ \sum_{i=1 }^{n} \sum_{a=1 }^{k} \frac{Z_{a} }{r_{ia}}
$$

第一項為電子的動能,第二項是電子與電子potential項,第三項電子與核之間的potential項.

The first term represents the kinetic energy of the electrons, the second term is the electron–electron potential, and the third term is the electron–nucleus potential.

電子與電子相互作用的存在無法使用分離變量去解上述的Schrödinger equation,因此引入平均場近似,用一個局域的potential來描述和其他電子的作用,這個potential和核之間的potential疊加形成的potential,就是獨立電子在晶格中運動所受到的平均場。

The presence of electron–electron interactions prevents the use of separation of variables to solve the above Schrödinger equation. Therefore, a mean-field approximation is introduced: a local potential is used to describe the interaction with other electrons. This potential, combined with the electron–nucleus potential, forms the effective potential that an independent electron experiences while moving in the lattice — i.e., the mean field.

$$
下面為電子在晶格中運動所受到的平均場,這表示電子(位在r_{i})和每一個離子實間(位在R_{j})的相互作用,即kohn-sham{}potential:
$$

$$
Below{}is{}the{}mean{}field{}experienced{}by{}an{}electron{}in{}the{}lattice,{}representing{}the{}interaction{}between{}an{}electron{}(at{}r_{i}){}and{}each{}ion{}(at{}R_{j}),{}i.e.,{}the{}Kohn–Sham{}potential:
$$

$$
V_{el_{r_{i}-R_{j}} } =\sum_{ij}^{}[-V_{K-S}(r_{i}-R_{j})\hat{r_{j} }]=\sum_{i=1 }^{n} \sum_{a=1 }^{k} \frac{Z_{a} }{r_{ia}} +\sum_{i=1 }^{n} \sum_{j>i }^{n} \frac{1 }{r_{ij}}
$$

因此電子的Hamilton的最終形式為:

Therefore, the final form of the electronic Hamiltonian is:

$$
\widehat{H}_{electron}= -\sum_{i=1}^{n} \frac{\nabla^{2} _{r_{i}  }  } {2} +V_{el_{r_{i}-R_{j}} } =-\sum_{i=1}^{n} \frac{\nabla^{2} _{r_{i}  }  } {2}+\sum_{ij}^{}[-V_{K-S}(r_{i}-R_{j})\hat{r_{j} }]
$$

$$
現在考慮晶格(離子實間)的振動產生的擾動對電子的Hamilton的影響,若振動較為微弱(偏離平衡點的距離很短),其相對位移為r_{j},電子(位在r_{i})和離子實的相互作用改為:
$$

$$
Now{}consider{}the{}effect{}of{}lattice{}(ion){}vibrations{}on{}the{}electronic{}Hamiltonian.{}If{}the{}vibrations{}are{}small{}(the{}displacement{}from{}equilibrium{}is{}very{}short),{}with{}relative{}displacement{}r_{j},{}the{}interaction{}between{}an{}electron{}(at{}r_{i}){}and{}the{}ions{}b
$$

$$
V_{el_{r_{i}-R_{j}+r_{j}} } =\sum_{ij}^{}[-V_{K-S}(r_{i}-R_{j}+r_{j} )\hat{r_{j} }]
$$

$$
對代表離子實平衡位置R_{j}展開晶格位移r_{j}對kohn-sham{} potential的修正項(泰勒展開式):
$$

$$
Expand{}the{}lattice{}displacement{}r_{j}{}around{}the{}ion{}equilibrium{}position{}R_{j}{}to{}obtain{}the{}correction{}term{}to{}the{}Kohn–Sham{}potential{}(Taylor{}expansion):
$$

$$
V_{el_{r_{i}-R_{j}+r_{j}} }=-V_{K-S }(r_{i}-R_{j}+r_{j}) =\sum_{ij}^{}[-V_{K-S}(r_{i}-R_{j})\hat{r_{j} }-\frac{\partial V_{K-S}(r_{i}-R_{j})}{\partial R_{j} } r_{j}\hat{r_{j}} -\frac{1}{2!}\frac{\partial^{2}  V_{K-S}(r_{i}-R_{j})}{\partial^{2} R_{j}} (r_{j})^{2} \hat{r_{j}} +...  ]
$$

$$
第一項為電子和固定晶格的相互作用,即週期性potential,第二項代表晶格偏移平衡位置引起的potential變化,其中R_{j}為平衡點的位置(lattice的晶格向量),r_{j}是晶格偏離平衡點位置的位移.
$$

$$
The{}first{}term{}represents{}the{}interaction{}between{}the{}electron{}and{}the{}fixed{}lattice,{}i.e.,{}the{}periodic{}potential.{}The{}second{}term{}represents{}the{}change{}in{}potential{}caused{}by{}the{}lattice{}displacement{}from{}equilibrium,{}where{}R_{j}{}is{}the{}equilibrium{}position{}(lattice{}vector){}and{}r_{j}{}is{}the{}displacement{}of{}the{}lattice{}from{}its{}equilibrium{}position.
$$

$$
V_{el_{r_{i}-R_{j}+r_{j}} }=-V_{K-S }(r_{i}-R_{j}+r_{j}) =\sum_{ij}^{}[-V_{K-S}(r_{i}-R_{j})\hat{r_{j} }-\frac{\partial V_{K-S}(r_{i}-R_{j})}{\partial R_{j} } r_{j}\hat{r_{j}} -\frac{1}{2!}\frac{\partial^{2}  V_{K-S}(r_{i}-R_{j})}{\partial^{2} R_{j}} (r_{j})^{2} \hat{r_{j}} +...  ]
$$

$$
第二項代表的物理意義可以從位能和微分的基本意義出發:-V_{K-S}(r_{i}-R_{j})代表電子從原子核位置R_{j} 移動到r_{i}時庫侖力做的功,對R_{j}微分表明當原子核位置微小變化時,電子和原子核間的勢能做功變化,從力學的角度來說,當原本固定的位能產生變化時,表示兩者間受到了一股額外的吸引力,現在\frac{\partial V_{K-S}(r_{i}-R_{j})}{\partial R_{j} }乘上原子核的位移r_{j}後,為系統因這股額外的力產生的Energy變化,與就是Hamilton的修正項,描述小位移對電子的線性影響,對應「電子-聲子耦合」（electron-phonon{} coupling）:
$$

$$
The{}physical{}meaning{}of{}the{}second{}term{}can{}be{}understood{}from{}the{}basic{}concepts{}of{}potential{}energy{}and{}differentiation:{}-V_{K-S}(r_{i}-R_{j}){}represents{}the{}work{}done{}by{}the{}Coulomb{}force{}when{}an{}electron{}moves{}from{}the{}atomic{}nucleus{}at{}R_{j}{}to{}r_{i}.{}Differentiation{}with{}respect{}to{}R_{j}{}indicates{}that{}when{}the{}atomic{}nucleus{}position{}changes{}slightly,{}the{}work{}done{}by{}the{}potential{}energy{}between{}the{}electron{}and{}nucleus{}changes.{}From{}a{}mechanical{}perspective,{}when{}the{}originally{}fixed{}potential{}changes,{}it{}means{}that{}an{}additional{}attractive{}force{}exists{}between{}the{}two.{}Now,{}multiplying{}\frac{\partial{}V_{K-S}(r_{i}-R_{j})}{\partial{}R_{j}}{}){}by{}the{}atomic{}displacement{}r_{i}{}gives{}the{}energy{}change{}of{}the{}system{}due{}to{}this{}additional{}force,{}which{}is{}the{}correction{}term{}of{}the{}Hamiltonian,{}describing{}the{}linear{}effect{}of{}small{}displacements{}on{}the{}electrons,{}corresponding{}to{}the{}electron–phonon{}coupling.
$$

$$
The{}physical{}meaning{}of{}the{}second{}term{}can{}be{}understood{}from{}the{}basic{}concepts{}of{}potential{}energy{}and{}differentiation:{}-V_{K-S}(r_{i}-R_{j}){}represents{}the{}work{}done{}by{}the{}Coulomb{}force{}when{}an{}electron{}moves{}from{}the{}atomic{}nucleus{}at{}R_{j}{}to{}r_{i}.{}Differentiation{}with{}respect{}to{}R_{j}{}indicates{}that{}when{}the{}atomic{}nucleus{}position{}changes{}slightly,{}the{}work{}done{}by{}the{}potential{}energy{}between{}the{}electron{}and{}nucleus{}changes.{}From{}a{}mechanical{}perspective,{}when{}the{}originally{}fixed{}potential{}changes,{}it{}means{}that{}an{}additional{}attractive{}force{}exists{}between{}the{}two.{}Now,{}multiplying{}$\frac{\partial{}V_{K-S}(r_{i}-R_{j})}{\partial{}R_{j}}${}by{}the{}atomic{}displacement{}r_{i}{}gives{}the{}energy{}change{}of{}the{}system{}due{}to{}this{}additional{}force,{}which{}is{}the{}correction{}term{}of{}the{}Hamiltonian,{}describing{}the{}linear{}effect{}of{}small{}displacements{}on{}the{}electrons,{}corresponding{}to{}the{}electron–phonon{}coupling.
$$


$$
V_{el-ph }=-\frac{\partial V_{K-S}(r_{i}-R_{j})}{\partial R_{j} } r_{j}\hat{r_{j}} 
$$

$$
將electron-phonon{} coupling項中的\hat{r_{j}}進行Fouier{}transfrom (實空間\longrightarrow q空間),再將r_{j}以phonon的算符形式展開:
$$

$$
Perform{}a{}Fourier{}transform{}of{}\hat{r_{j}}{}in{}the{}electron–phonon{}coupling{}term{}(real{}space{}\longrightarrow{}q-space),{}and{}then{}expand{}r_{j}{}in{}terms{}of{}phonon{}operators:
$$

$$
r_{j}\hat{r_{j}}=\frac{1}{\sqrt{N_{p} } } \sum_{q}^{} e^{iqR_{j}  } r_{j}\hat{r_{q}} = \frac{1}{\sqrt{N_{p} } } \sum_{q}^{} e^{iqR_{j}  }\frac{1}{\sqrt{2M\omega _{q}} }(a_{q}+a_{-q}^{+}) \hat{r_{q}}
$$

$$
其中\hat{r_{q}}和\omega _{q}和是phonon的eigenvector和頻率,可從計算phonon的dynamics matrix得到(supplementary1),N_{p}為單位晶格的數量.
$$

$$
Here,{}\hat{r_{q}}{}and{} \omega _{q}{}are{}the{}phonon{}eigenvectors{}and{}frequencies,{}which{}can{}be{}obtained{}from{}the{}phonon{}dynamical{}matrix{}calculation{}(Supplementary{}1).{}N_{p}{}is{}the{}number{}of{}unit{}cells.
$$

$$
同時對電子的kohn-sham{} potential{}V_{K-S}(r_{i}-R_{j})進行Fouier{}transfrom (實空間\longrightarrow k空間):
$$

$$
At{}the{}same{}time,{}perform{}a{}Fourier{}transform{}of{}the{}electronic{}Kohn–Sham{}potential{}V_{K-S}(r_{i}-R_{j}){}(real{}space{}\longrightarrow{}k-space):
$$

$$
V_{K-S}(r_{i}-R_{j})=-\frac{1}{\sqrt{N_{p} } } \sum_{k}^{} e^{ik(r_{i}- R_{j})} V_{K-S}(k)
$$

$$
對Fouier{}transfrom後的kohn-sham{} potential{}V_{K-S}(r_{i}-R_{j})偏微分:
$$

$$
Take{}the{}partial{}derivative{}of{}the{}Fourier-transformed{}Kohn–Sham{}potential{}V_{K-S}(r_{i}-R_{j}):
$$

$$
\frac{\partial V_{K-S}(r_{i}-R_{j})}{\partial R_{j} }=-\frac{1}{\sqrt{N_{p} } } ike^{ik(r_{i}-R_{j})}V_{K-S}(k)
$$

$$
將r_{j}\hat{r_{j}}和V_{K-S}(k)代回V_{el_{-ph} }中,可以得到V_{el_{-ph} }新的形式:
$$

$$
V_{el_{-ph} } = -ik\sum_{ij}^{}  \frac{1}{N_{p} } \sum_{q}^{} e^{i(qR_{j}+kr_{i} -kR_{j})  }\frac{1}{\sqrt{2M\omega _{q}} }(a_{q}+a_{-q}^{+})V_{K-S}(k)  \hat{r_{q}}
$$

$$
可以看到,當k=q時,V_{el_{-ph} }可變為:
$$

$$
-ik\frac{1}{N_{p} } \sum_{i,q}^{} e^{ikr_{i} }\frac{1}{\sqrt{2M\omega _{q}} }(a_{q}+a_{-q}^{+})V_{K-S}(k)  \hat{r_{q}}=-iq\sum_{i,q}^{}\frac{1}{N_{p} }e^{iqr_{i} }\frac{1}{\sqrt{2M\omega _{q}} }(a_{q}+a_{-q}^{+})V_{K-S}(q)  \hat{r_{q}} 
$$

$$
接下來計算系統的總el-ph的能量密度,將V_{el_{-ph} }以kohn-sham{}orbital(\hat{\psi}_{n^{'} ,k^{'}})展開,得H_{el-ph}:
$$

$$
H_{el-ph}=\sum_{n,k;n^{'},k^{'} }^{}\hat{\psi}_{n ,k}^{+} V_{el}\hat{\psi}_{n^{'} ,k^{'}}
$$

在週期性的系統中,波函數滿足Bloch定理,因此可以將kohn-sham state以布洛赫波的二次量子化形式展開:

$$
\hat{\psi}_{n^{'} ,k^{'}}  =\frac{e^{ik^{'}.r} }{\sqrt{N_{p}} }C_{n_{'},k_{'}}|\psi _{n^{'} ,k^{'}}>;\hat{\psi}_{n ,k}^{+}   =\frac{e^{-ik.r} }{\sqrt{N_{p}} }C_{n,k}^{+}|\psi _{n ,k}>
$$

$$
將其代入H_{el-ph}中,得:
$$

$$
H_{el-ph}=\sum_{i,q}^{}\sum_{n,k;n^{'},k^{'}}^{}  < \psi _{n,k}|-iq\frac{1}{N_{p} }e^{i(k+q-k^{'} )r_{i} }\frac{1}{\sqrt{2M\omega _{q}} }(a_{q}+a_{-q}^{+})V_{K-S}(q)   |\psi _{n^{'} ,k^{'}}> C_{n,k}^{+}C_{n^{'} ,k^{'}}\hat{r_{q}}  
$$

$$
定義G_{q}為:
$$

$$
G_{q}=-\frac{1}{N_{p} }\frac{1}{\sqrt{2M\omega _{q}} }iqe^{i(k+q-k^{'} )r_{i} }(a_{q}+a_{-q}^{+})V_{K-S}(q)\hat{r_{q}}
$$

$$
根據Phonon{}dispersion,可知acoustic{}mode 在q=0時,\omega _{q}=0,即晶格不在振動,同時el-ph項將變為0,Hamilton將退化回Born-Oppenheimer{}approximation近似的形式.
$$

$$
將H_{el-ph}轉為 electron-phonon{}Hamilton 的二次量子化形式:
$$

$$
H_{el-ph}=\sum_{qknn^{'}}^{} \langle n,k | G_q | n', k' \rangle C_{n,k}^{+}C_{n^{'} ,k^{'}}=\sum_{qknn^{'}}^{} g_{nn^{'}}(k,q)C_{n,k}^{+}C_{n^{'} ,k^{'}}
$$

$$
g_{nn^{'}}(k,q)為電聲耦合矩陣元素,描述受到phonon散射後,電子從state(n^{'} ,k^{'})散射到state(n^{'} ,k^{'})的概率幅:
$$

$$
\langle n,k | G_q | n', k' \rangle=g_{nn^{'}}(k,q)
$$

電聲耦合的物理圖像可以想成:電子在晶格中移動,會吸引正電荷(離子實),造成晶格畸變,釋放一個phonon,形成一個局域正電荷密集區,而這個電子的state也會改變(散射);另一個電子經過局域正電荷密集區,會吸收這個phonon,受到吸引,變成另一個state(散射)

$$
若將系統的聲子和電子Hamilton和electron-phonon{}Hamilton加起來,其為系統的total{}Hamilton(Frohlich{}Hamilton):
$$

$$
H =H_{0}+H_{el-ph}=\sum_{q}^{}\hbar \omega _{q}a_{-q}^{+}a_{q}+\sum_{k}^{}\varepsilon _{k} C_{n,k}^{+}C_{n^{'} ,k^{'}}+\sum_{qknn^{'}}^{} g_{nn^{'}}(k,q)C_{n,k}^{+}C_{n^{'} ,k^{'}}
$$

$$
H_{0}=H_{phonon}+ H_{electron}=\sum_{q}^{}\hbar \omega _{q}a_{-q}^{+}a_{q}+\sum_{k}^{}\varepsilon _{k} C_{n,k}^{+}C_{n^{'} ,k^{'}}
$$

$$
H_{el-ph}=\sum_{qknn^{'}}^{} g_{nn^{'}}(k,q)C_{n,k}^{+}C_{n^{'} ,k^{'}}
$$

電聲耦合常數在特定條件下,將變成負值,表示電聲耦合常數將讓兩個受到散射的電子間存在吸引勢.根據BCS理論,若兩個電子間存在吸引勢將促使兩個電子形成庫柏對,也就是超導的來源.

若要瞭解電聲耦合常數將在何時變成負值,需要進行正則變換(canonical transformation),進一步分析!
